pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        // 차트 코드 추가
        maven { url 'https://jitpack.io' }
    }
}


dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // 차트 코드 추가
        maven { url 'https://jitpack.io' }
    }
}


rootProject.name = "Keepers_Test"
include ':app'
